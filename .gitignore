node_modules/
dist/
*.local
.DS_Store
.vite/

# Throwaway probe files a review agent drops in to reproduce a bug. They are
# scratch, not part of the suite, and should never be committed.
__probe*
*.probe.ts
